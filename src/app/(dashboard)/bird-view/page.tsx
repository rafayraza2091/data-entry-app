'use client';

import { useState } from 'react';

export default function BirdViewPage() {
  const [loading, setLoading] = useState(false);

  return (
    <div className="flex-1 -m-7 bg-white rounded-xl shadow-sm border border-gray-100 flex flex-col items-center justify-center text-center animate-fadeIn overflow-hidden">
      {/* Empty for now as requested */}
    </div>
  );
}
