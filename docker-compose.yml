version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "3005:3005"
    environment:
      - DATABASE_URL=postgresql://admin:1234@host.docker.internal:5432/myAcademy_dataentry?schema=public